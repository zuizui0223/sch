# SCH execution candidate ranking v2

## Ranking rule

This ranking is for the **shortest path to a causal Chapter-1 compromise result**, not for source richness.

A strong system should provide:

```text
C1  one interpretable shared coordinate z
C2  multi-level manipulation of z
C3  separately measurable functions
C4  evidence for opposing causal geometry
C5  one common fitness endpoint
C6  selective manipulation of functional weights
C7  short function -> fitness paths
C8  context / evolutionary extension
C9  conflict active in the focal population / season.
```

The default empirical targets are state-specific:

```text
z_P* = argmax W10(z)
z_G* = argmax W01(z)
z_C* = argmax W11(z).
```

Pure function optima are a separate promotion lane.

## Rank 1 conditional — Dalechampia

Why it remains first:

- strongest existing case-level stabilizing compromise on a showy bract/display coordinate;
- pollinator benefit and seed-predator cost already oppose one another;
- common seed fitness is available;
- display manipulation has programme-level precedent.

Main bottlenecks:

```text
multi-level z manipulation in the same focal system: PARTIAL
selective antagonist intervention: UNRESOLVED
focal conflict activity: MUST BE SCREENED.
```

Current status:

```text
CONDITIONAL_FIRST_CHOICE_CAUSAL_COMPROMISE_SYSTEM
```

Dalechampia remains first only when a conflict-active population and a selective G contrast are recovered.

## Rank 2 — Pedicularis rex

Pedicularis now outranks Castilleja because one of the hardest SCH requirements — selective manipulation of the antagonist-facing functional weight — is already experimentally demonstrated.

### Shared-conflict evidence

The 2016 multipopulation study shows:

```text
corolla exsertion -> pollen arrival: positive
corolla exsertion -> seed predation: positive
seed predation -> final viable seeds: negative.
```

Thus pollination favors greater exsertion while seed-predator protection favors reduced exsertion.

Antagonist intensity varies strongly geographically:

```text
seed predation range = 0.80% to 27.42%
```

while the pollinator-facing direction is comparatively stable.

### Selective functional-weight intervention

The 2015 bract-water experiment provides an unusually strong C6 result.

Draining the water-bearing bracts:

```text
pollinator visitation treatment:
beta = 0.012, P = 0.958

initial seed set treatment:
beta = 0.001, P = 0.906

seed predation treatment:
beta = -0.072, P < 0.0001

final seed set treatment:
beta = 0.025, P < 0.0001.
```

The source states that drainage increased seed predation; the treatment effect on predation was significant in 5/6 populations.

This is close to a selective G-weight manipulation:

```text
strong antagonist effect
+ negligible detected pollinator-visitation effect.
```

### Current score

```text
C1 shared z:                    STRONG / composite geometry
C2 >=3-level z manipulation:    MAIN BOTTLENECK
C3 separate functions:          STRONG
C4 opposing geometry:           STRONG OBSERVATIONAL SUPPORT
C5 common fitness:              STRONG
C6 selective function weights:  STRONG FOR WATER-DEFENCE AXIS
C7 short paths:                 STRONG
C8 context extension:           STRONG GEOGRAPHIC MOSAIC
C9 focal conflict active:       LIKELY BUT MUST BE SCREENED.
```

Current status:

```text
BEST_SELECTIVE_WEIGHT_MANIPULATION_CANDIDATE
BEST_SHARED_CONFLICT_TO_SECOND_AXIS_REALITY_BRIDGE
```

The decisive missing step is a validated multi-level manipulation of exsertion itself.

## Rank 3 — Castilleja linariaefolia

Strengths:

- hummingbird pollination and pre-dispersal seed predation are distinct pathways;
- opposing selection on calyx length is recovered;
- antagonist -> seed-loss path is short;
- common female fitness is natural.

Weaknesses:

```text
direct multi-level calyx manipulation: NOT RECOVERED
selective predator intervention: NOT RECOVERED.
```

Current status:

```text
HIGH_VALUE_SHORT_PATH_CANDIDATE
```

Castilleja remains a strong fallback if a clean z manipulation can be built, but Pedicularis now has the stronger selective-function intervention precedent.

## Rank 4 — Polemonium viscosum

Best non-antagonist broad conflict candidate:

```text
function 1 = pollination
function 2 = water economy / viability
z          = flower size / morphology.
```

Strength: generalizes SCH beyond mutualist-antagonist systems.

Weakness: common total fitness and direct multi-level z manipulation remain harder.

Status:

```text
BEST_BROAD_PHYSIOLOGICAL_CONFLICT_CANDIDATE
```

## Rank 5 — Nicotiana attenuata

Still the strongest local shared-cue and direct SCH -> BITA attraction-defence bridge.

Strengths:

- manipulable benzylacetone axis;
- same A affects pollinator-mediated reproduction and hawkmoth oviposition;
- pollen-loading and egg-removal intervention components exist;
- direct continuity into BITA A x D.

Weaknesses:

- antagonist -> focal common fitness is comparatively long;
- one hawkmoth can occupy both functional roles;
- combined P/G selectivity remains unresolved;
- multi-level internal compromise surface is not yet recovered.

Status:

```text
FIRST_CHOICE_LOCAL_MECHANISM_SYSTEM
FIRST_CHOICE_ATTRACTION_DEFENCE_HANDOFF
```

## Negative / control systems

### Ipomopsis aggregata

Direct display manipulation affects pollinators, seed predators and female fitness, but the tested range is largely monotonic rather than an identified compromise.

```text
NEAR_COMPLETE_MONOTONIC_CONTROL
```

### Platycodon / aligned flower orientation

One trait affects two functions but both favor the same state.

```text
MULTIFUNCTIONALITY_WITHOUT_CONFLICT_CONTROL
```

## Updated execution lanes

### Lane A — primary causal compromise

```text
Dalechampia
-> focal-population screen
-> reversible multi-level z
-> selective G
-> z x P x G
-> state-specific compromise receipt.
```

### Lane B — strongest alternative with selective function control already recovered

```text
Pedicularis
-> qualify conflict-active population
-> develop multi-level exsertion manipulation
-> reuse / adapt validated water-defence manipulation
-> cross z x P x G
-> test geographic movement of compromise as antagonist weight changes.
```

### Lane C — short antagonist-to-fitness fallback

```text
Castilleja
-> develop direct calyx manipulation
-> develop selective predator intervention
-> common seed fitness.
```

### Lane D — direct attraction-defence Chapter-2 continuity

```text
Nicotiana
-> same A local conflict
-> combined P/G selectivity
-> direct BITA A x D continuation.
```

## Main conclusion

The ranking is now:

```text
1  Dalechampia   strongest existing compromise geometry, conditional on focal context / G control
2  Pedicularis   strongest selective functional-weight manipulation + shared-conflict bridge
3  Castilleja    strongest short G -> seed pathway after manipulation development
4  Polemonium    broad non-antagonist conflict
5  Nicotiana     strongest local shared-cue / BITA attraction-defence continuity.
```

The important change is that **Pedicularis is no longer just an external validation case**. Its water-bract experiment solves a major intervention problem and makes it a serious causal-compromise execution candidate once the exsertion coordinate can be manipulated over multiple levels.
