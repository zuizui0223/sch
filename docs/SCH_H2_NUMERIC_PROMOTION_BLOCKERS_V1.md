# SCH H2 numeric promotion blockers V1

Three biologically strong systems were re-audited specifically against the registered commensurate-estimand gate.

## Result

None is promoted from directional/source-resolved geometry to a new TOTAL_SELECTION_EFFECT cluster yet.

| programme | ecological evidence | numeric promotion blocker |
| --- | --- | --- |
| Castilleja linariaefolia | calyx-length conflict on relative seed set is source-verified | exact table/path coefficients plus uncertainty not materialized |
| Collaea cipoensis | flower size and flower number have opposing pollinator/antagonist contributions on female fitness | exact trait-specific component/net coefficients plus uncertainty not materialized |
| Pedicularis rex | same-axis conflict and geographic heterogeneity are source-verified | supporting XLS/population objects are identified but not materialized as commensurate selection coefficients |

Collaea record SCHPRISMA-000353 belongs to the same biological programme as SCHPRISMA-000312 and is not an independent cluster.

## Consequence

The registered state remains:

~~~text
TOTAL_SELECTION_EFFECT independent clusters = 6 / 8
STANDARDIZED_SELECTION_GRADIENT clusters     = 5
~~~

Directional prose, significance statements, pressure percentages, and dependent publications cannot be substituted for the missing numeric estimand.

## Next search rule

Prioritize already-included or frozen-cohort studies where the primary source exposes a machine-readable or directly transcribed table containing:

1. the same focal plant trait coordinate;
2. total or component selection tied to a common fitness endpoint;
3. an explicit coefficient scale;
4. uncertainty where the registered numeric family requires it;
5. a biological programme not already represented among the six clusters.

This is an estimand-recovery search, not a literature-volume search.
