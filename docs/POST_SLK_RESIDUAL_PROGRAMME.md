# SCH residual programme after SLK integration

The integrated cross-repository flagship spine has moved conceptually to [`zuizui0223/slk`](https://github.com/zuizui0223/slk).

SLK uses only a bounded SCH handoff:

```text
valid shared-coordinate conflict receipt
-> conflict budget L
```

SCH remains the canonical home for the harder identification problem that precedes that handoff.

## Primary residual paper

Working thesis:

**Causal identification of functional conflict in multifunctional traits.**

Core claims to develop:

1. `multifunctionality != conflict`;
2. state-specific optima such as `z_P*` and `z_G*` are not automatically pure-function optima;
3. crossed interventions identify contextual compromise geometry;
4. component contrasts plus prospective context-stability criteria can promote contextual optima to empirical pure-function optima;
5. failure of the promotion gate is itself an interpretable result rather than a failed experiment.

## Empirical spine

Retain and develop:

- Dalechampia Stage 0 qualification;
- reversible multi-level `z` manipulation;
- selective pollinator/antagonist interventions;
- `5 z x 2 P x 2 G` compromise-surface design;
- context-stable component-optimum upgrade;
- real-world evidence synthesis as biological support rather than estimand definition.

## Material no longer needing to carry SCH's main novelty

The cross-architecture classification `L -> Phi=sL-K -> evolutionary outcomes` should be cited from SLK rather than re-expanded as SCH's main paper.

SCH should become narrower and more empirical/identification-focused as SLK develops.